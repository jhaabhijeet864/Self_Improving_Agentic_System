# Jarvis-OS Requirements
# Core dependencies

# No external dependencies required for core functionality
# All components use only Python standard library

# Optional dependencies for enhanced features:
# For async support (already in stdlib)
# For logging (already in stdlib)

# Dashboard dependencies (Phase 1)
fastapi>=0.95.0
uvicorn>=0.21.0

# Development and testing
pytest>=7.0.0
pytest-asyncio>=0.18.0
pytest-cov>=3.0.0

# Code quality
black>=22.0.0
pylint>=2.0.0
mypy>=0.950

# Documentation
sphinx>=4.0.0
sphinx-rtd-theme>=1.0.0
