from setuptools import setup, find_packages
setup(
    name="jarvis_os",
    version="1.0.0",
    description="Autonomous AI Agent Framework",
    # This automatically finds your python files in the folder
    packages=find_packages(), 
)
